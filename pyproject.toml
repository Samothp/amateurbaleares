[project]
name = "amateurbaleares"
version = "0.1.0"
description = "Proyecto inicial de Python para AmateurBaleares"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
amateurbaleares = "src.app:main"
ffib-scraper = "src.scraper.ffib_cli:main"
